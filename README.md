# MateriaGris Ecommerce BackEnd

First install dependencies to run the project:

```cmd
npm instal
```

To run server in development mode use:

```cmd
npm run dev
```

To run server in production mode use:

```cmd
npm start
```
